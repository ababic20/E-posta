const UserDAO = require("./userDAO.js");

class Encryption {

    getSenderPrivateKey = function (senderID) {
        let uDao = new UserDAO();
        uDao.getUser

    }

    getRecipeintPublicKey = function (recipeintID) {

    }
}
module.exports = Encryption;